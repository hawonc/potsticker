version: '3.8'

services:
  pots:
    build: .
    container_name: pots-server
    ports:
      - "3000:3000"
    volumes:
      - ./access_log.txt:/app/access_log.txt
    restart: unless-stopped
    network_mode: bridge
